spring.application.name=backend

# MySQL connection (DB name ASD)
spring.datasource.url=jdbc:mysql://localhost:3306/ASD
spring.datasource.username=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Force dialect (prevents dialect-detection failure)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


server.port=8081

# mail details
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=6be7287feaa23f
spring.mail.password=960ac69c05b80d
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# frontend url
app.frontend.base-url=http://localhost:5173

#jwt secret
jwt.secret=mysecretkeymysecretkeymysecretkey12

